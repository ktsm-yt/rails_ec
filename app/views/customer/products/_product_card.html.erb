<div class="col mb-5">
  <div class="card h-100">
    <% if product.on_sale? %>
      <!-- Sale badge-->
      <div class="badge bg-dark text-white position-absolute" style="top: 0.5rem; right: 0.5rem">Sale</div>
    <% end %>
    <!-- Product image-->
    <img class="card-img-top" src="<%= product.image_url %>" alt="<%= product.name %>" />
    <!-- Product details-->
    <div class="card-body p-4">
        <div class="text-center">
            <!-- Product name-->
            <h5 class="fw-bolder"><%= product.name %></h5>
            <!-- Product reviews-->
            <%= if product.rating && product.rating.to_i > 0 %>
              <div class="d-flex justify-content-center small text-warning mb-2">
                <% product.rating.to_i.times do %>
                  <div class="bi-star-fill"></div>
                <% end %>
              </div>
            <!-- Product price-->
            <% if product.on_sale? && product_old_price %>
              <span class="text-muted text-decoration-line-through"><%= number_to_currency(product.old_price) %></span>
            <% end %>
            <% if product.price_range? %>
              <%= product.price %>
            <% else %>
              <%= number_to_currency(product.price) %>
            <% end %>
        </div>
    </div>
    <!-- Product actions-->
    <div class="card-footer p-4 pt-0 border-top-0 bg-transparent">
      <div class="text-center">
      <% if product.price_range? %>
        <a class="btn btn-outline-dark mt-auto" href="">View options</a></div>
      <% else %>
        <a class="btn btn-outline-dark mt-auto" href="#">Add to cart</a></div>
      <% end %>
    </div>
  </div>
</div>